'''Problem Statement:
You are given a list of integers.
You need to count how many pairs of indices (i, j) exist such that:

i < j, and

arr[i] == arr[j]

Input Format:

First line: an integer n (number of elements in the list)

Second line: n space-separated integers

Output Format:

Print a single integer — the total number of equal pairs.'''

n = int(input())

list= input().split(" ")
count=0

for i in range(len(list)):
    for j in range(i+1,len(list)):
        if(list[i]==list[j]):
            count+=1

print(count)
